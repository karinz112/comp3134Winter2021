1. I typed Ben and got no result because active = 0 for Ben inside the database, but search looks for active = 1.

2. I couldn't find a result as the input I used is out of the list in the database. 

3.I could see all the information from the database because it isn't sanitized and this can be easily used.